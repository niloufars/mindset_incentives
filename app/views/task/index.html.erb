averages: <br>

<br><br>
<% (0..@all_users.length-1).each do |i| %>
	<%= @all_users[i] %>, <%= @all_conditions[i] %><br>
	<ul>
	<% @tasks.where(workerID: @all_users[i]).order(:id).each do |task| %>
		<li>
		task: <%= task.tasktype %> | stage: <%= task.taskstage %> | <%= task.accuracy %>% | $<%= task.bonus %>
		</li>
	<% end %>
	</ul>
<% end %>